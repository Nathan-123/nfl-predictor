# Data regeneratable via the pipeline -- ignore the cache dirs specifically
# (not /data/ wholesale) so /data/manual/ (small, hand-curated, worth
# committing) stays tracked.
/data/raw/
/data/processed/

# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Virtual environments
venv/
.venv/

# Editors
.vscode/
.idea/
